## 🤖 AI Change Summary for PR #44

### Chunk 1/1
### 개요
이 변경 사항은 `ItemDto`, `Item`, `MemberService`, 및 `StoreService` 클래스에 대한 수정으로, 재고 관리 및 상점 관련 기능을 개선합니다. 주요 변경 사항은 재고 수량을 관리하는 새로운 필드와 메서드 추가, 닉네임 변경 시 상점 제목 초기화 로직 추가, 그리고 아이템 구매 시 재고 감소 로직을 포함합니다.

### 주요 변경 사항
1. **ItemDto.java**
   - `sellCounts` 필드 추가: 재고 수량을 나타내며, 여러 JSON 필드 이름에 대한 별칭을 설정.
   
2. **Item.java**
   - `imageUrl` 필드의 nullable 속성을 `true`로 변경.
   - `addQuantity` 메서드에 재고가 0보다 클 경우 `isAvailable`을 `true`로 설정하는 로직 추가.
   - `decreaseQuantity` 메서드 추가: 재고를 감소시키고, 재고가 0일 경우 `isAvailable`을 `false`로 설정.

3. **MemberService.java**
   - 닉네임 변경 시 이전 닉네임을 확인하고, 최초 설정 시 상점 제목을 초기화하는 로직 추가.

4. **StoreService.java**
   - 아이템 구매 시 재고가 없거나 사용 불가능한 경우 예외 처리 추가.
   - 아이템 생성 시 `sellCounts`를 사용하여 재고 수량을 설정하고, 아이템이 존재할 경우 재고를 증가시키는 로직 추가.
   - 아이템 생성 요청 유효성 검사에서 `sellCounts`가 음수일 경우 예외 처리 추가.

### 위험/영향
- **기능적 위험**: 재고 관리 로직이 변경됨에 따라 기존의 재고 관련 기능에 영향을 줄 수 있으며, 특히 재고가 0일 때의 처리 로직이 중요해짐.
- **호환성**: 새로운 필드와 메서드가 추가됨에 따라 기존 API 소비자에게 영향을 줄 수 있음.
- **테스트 필요성**: 변경된 로직에 대한 충분한 테스트가 필요하며, 특히 예외 처리와 재고 관리 관련 기능에 대한 검증이 중요.

### 테스트/검증
- 새로운 `sellCounts` 필드와 관련된 JSON 직렬화/역직렬화 테스트 필요.
- `addQuantity` 및 `decreaseQuantity` 메서드의 동작을 검증하기 위한 단위 테스트 추가.
- 닉네임 변경 시 상점 제목 초기화 로직에 대한 통합 테스트 필요.
- 아이템 구매 시 재고 상태에 따른 예외 처리 테스트 필요.

### 후속 조치
- 변경 사항에 대한 문서화 필요.
- 기존 테스트 케이스 업데이트 및 새로운 테스트 케이스 작성.
- API 소비자에게 변경 사항 통지 및 필요한 경우 API 버전 관리 고려.
