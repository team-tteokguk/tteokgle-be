## 🤖 AI Change Summary for PR #36

### Chunk 1/1
### 개요
이 변경 사항은 `NotificationRepository`와 `NotificationService`에서 알림을 읽음 처리하는 방법을 수정하여, 특정 회원의 모든 읽지 않은 알림을 일괄적으로 읽음 처리하도록 개선하였습니다. 이로 인해 코드의 가독성과 유지보수성이 향상되었습니다.

### 주요 변경 사항
1. **NotificationRepository 수정**:
   - `updateNotification` 메소드를 `markAllAsReadByMemberId`로 변경하여, 특정 회원의 읽지 않은 알림만 업데이트하도록 쿼리를 수정하였습니다.

2. **NotificationService 수정**:
   - `readNotification` 메소드의 매개변수를 `notificationId`에서 `memberId`로 변경하고, 해당 회원의 모든 알림을 읽음 처리하는 로직으로 수정하였습니다.

3. **테스트 수정**:
   - `NotificationRepositoryTest`와 `NotificationServiceTest`에서 기존의 알림 읽음 처리 테스트를 새로운 메소드 호출로 변경하였습니다. 
   - `readNotification` 메소드의 테스트를 회원 ID를 사용하여 모든 알림을 일괄적으로 읽음 처리하는 방식으로 수정하였습니다.

### 위험/영향
- **기능적 변화**: 기존의 개별 알림 읽음 처리에서 특정 회원의 모든 읽지 않은 알림을 일괄적으로 처리하는 방식으로 변경되었으므로, 기존의 알림 처리 로직을 사용하는 다른 부분에 영향을 줄 수 있습니다.
- **호환성**: 기존의 `readNotification` 메소드를 호출하는 코드가 있는 경우, 해당 코드가 새로운 메소드 시그니처와 호환되지 않으므로 수정이 필요합니다.

### 테스트/검증
- 변경된 메소드에 대한 단위 테스트가 추가 및 수정되어, 새로운 로직이 올바르게 작동하는지 검증되었습니다. 
- 모든 테스트가 성공적으로 통과하여 기능이 정상적으로 작동함을 확인하였습니다.

### 후속 조치
- 기존의 `readNotification` 메소드를 호출하는 모든 코드의 수정이 필요합니다.
- 변경 사항에 대한 문서화 및 코드 주석 업데이트가 필요합니다.
- 추가적인 통합 테스트를 통해 전체 시스템에서의 동작을 검증할 필요가 있습니다.
